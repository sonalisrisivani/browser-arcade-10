{
  "name": "browser-arcade-10",
  "version": "1.0.0",
  "description": "Ten polished, independently playable browser games built with vanilla HTML/CSS/JS.",
  "private": true,
  "type": "commonjs",
  "scripts": {
    "check": "node scripts/check-syntax.mjs",
    "test": "node --test tests/",
    "serve": "python3 -m http.server 8080"
  },
  "devDependencies": {
    "jsdom": "^26.0.0"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "MIT"
}
